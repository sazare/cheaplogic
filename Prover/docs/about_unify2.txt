整理2

1. insideがx<-f(x)だけをテストするのは、unifyでdisagreeを順番に解消していくから
　　x<-f(y), y<-g(x)は結局x<-f(x)におちつくから

2. disagreeでいったん同一とみなされた部分は、つまり今のdisagreeより左側は
　どのような代入によっても同一になるので、本当は無視できる

　代入の*　つまりσ*ςの計算をどうするかが問題。

3. σの制約として、x<-e[x]となりうる組み合わせを除外することは可能か。コストとか。

■ Σを<v,e>の集合とする。vはシンボル、eは表現。
　任意の表現Eについて、E*Σ*Σ!=E*Σのとき、Σはfixed point, idempotentなどと呼ぶ。
　Σがfixed pointのとき、ある表現Eについて、E*Σ*Σ=E*Σとなることはあるだろうか。

つまり{x<-f(y), y<-g(z), z<-h(x)}みたいな場合だが、F=F(x,y,z)を考えれば

