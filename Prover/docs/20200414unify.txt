p記法とn記法について

p記法には順序がないとか

disagreeをcoroutineで実装して、性能あがるのかどうか?

unifyのmergeで、cp代入をつくりたい。
cp代入は、p代入であり
Vi(σ) in Ej(σ)ではないという条件を満たす代入。
順序に関係なくなるのでp代入の形にしてもいい。
vars←termsの形にしたいので、p代入の形を選ぶ

このcp代入は、Eにσが染み渡っているため、同じ記号に対して
σを一度だけ適用すればよい。

だから、resolventのapplyが高速にできる。

e*σ=e@σになってほしい。

そのようなσを作るにはμ←μ*δで染み渡らせる。のでよいか。
p記法でやるとき、問題ないか


δはdisagreeで作るv←eなのでμの変数は含まない。
同じe2の中のe2'とe2''があり、e1のxがe2'[x]にあるとき


