機能するということは、電流が流れるということか?
　+Work(x) <-> any p1,p2 +Curr(p1,x,p2)
ちょっとちがうかも。パーツによって機能するかどうかの判定方法が違うだろう。

p1,p2に電流が流れていたら、その間にLamp xを挿入するとxはwhiteになる。
　+WorkL(x) <-> any p1,p2 -Curr(p1,p2), +State(p1,p2,x, white)

これはまわりくどくないか?
本当はテスターを使って、xに電流が流れるかどうかを確認する。whiteは関係ない???
p1,p2はテスターの赤と黒。それは任意の電位差のある二点の代理になりうると、電磁学によって保証されている。
電磁学というのは、実験と理論の総体であり、何も保証しないのではないか?

製品dはlampとswが構成されていて、観測できる点p1,p2がある・・・
+Dev(d, p1, lamp, p2, sw)

p1,p2は機能としては見えないので、これはおかしい。

A) こう書くべきか
+Dev(d,lamp,sw)

あるいは,dとの関係を強調し

+Dev(d,d.lamp,d.sw)

とか??

回路の定義の詳細が省略できるのか??

B) lがbindされていればこれは言えるか?

+Curr(d,lamp,l) -> State(l,wh)
-Curr(d,lamp,l) -> State(l,bl)

d,lampはd.lampに同等か?

Curr(d,lamp,l)は、装置dの中のlampの位置にある特定のlについて
電流が流れているといっている。
それが成り立つにはdに対する条件が必要。

その条件をW(d)と書くとすると

-W(d) -> +Curr(d,lamp, l)
-W(d) -> +Curr(d,sw, s)

とかけるかもしれない。これは何も意味しない。

-W(d) -> +State(d,lamp, wh)
-W(d) -> +State(d,sw, on)

このときW(d)はなんかよくわからないので、この道はNG


# もしもp1,p2を残すと
# 
# +Curr(p1,lamp,l,p2) -> State(lamp,wh)
# -Curr(p1,lamp,l,p2) -> State(lamp,bl)
# Stateのlampはlかも。
# やはりp1,p2が必要とは思えない?
# 
# Currを考えるとき、部品の外側が必要なのでp1,p2もいれているが、
# どうか??

A)に戻って

CurrのlとStateのlは同じ名前だが、解釈が違うのでは?

lと{wh,bl}は等価か?



Workではそれは捨象する


ゆえに
+Curr(p1,lamp,l,p2) <-> State(lamp, l)

Work(lamp), Work(sw) -> Curr(p1,lamp, p2, sw)

Workは、ある条件のとき部品は正常に動くということ
正常というのは回路の仕様通りということ
それは回路がWorkともいえる

回路の構成によって状態が異なるというのは、回路の責任

Work(sw), Work(lamp)のもとで
たとえば(sw,lamp)=(on,wh)と(off,bl)の関係は回路の責任
入出力とみると sw => lamp
これを因果とみればswの状態変化とlampの状態変化に因果があることになる。

