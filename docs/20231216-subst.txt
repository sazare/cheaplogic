代入

{x/t, y/s} 
　条件 xはtに出現しない。yはsに出現しない。

eが表現とするとe[x]は、eのxに着目したメタ表記。

代入σ={x/f(z)} のとき、e=∀x∀yP(x,y)なら
 e・σ=∀yP(f(z),y)

　代入に出現する記号は変数定数の区別はない。/の上の記号は「置き換えられる記号」という意味であり
　変数を意味しない。実際には変数に対して用いるのではあるが。

実際には任意の論理式でなく、clause形式のみに適用するので量子記号はないし、



