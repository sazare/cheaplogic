;; define eqv of Exprs
;; this failes on 3. C5 is x

(1 () (- ≡ a a))
(2 () (- ≡ (f a x) (f a x)))
(3 () (+ ≡ a b))

((x) (+ ≡ x x) (- Expr x))
((x y) (- ≡ x y) (+ ≡ x y))

(readefile "kqc/explore/const/exprdef.kqc")

