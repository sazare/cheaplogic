;; define eqv of Exprs
;; this failes on 3. C5 is x

(1 () (- Expr= a a))
(2 () (- Expr= (f a x) (f a x)))
(3 () (+ Expr= a b))

((x) (+ Expr= x x) (- Expr x))
((x y) (- Expr= x y) (+ Expr= x y))

(readefile "kqc/explore/const/exprdef.kqc")

