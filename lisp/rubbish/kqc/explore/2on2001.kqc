;; 単純な例
;;; fact/変数ゾーン/ふたたび定数で分岐/goalのパターン
;;;; つまり、途中で変数がはいるために、あいまいさがでているのでは?

;; extractinf(z)をめざす
(1 (z) (- S z))


(() (+ P a))
(() (+ P b))
((x) (- P x)(+ R x))
(() (- R a) (+ S c))
(() (- R b) (+ S d))

;; これで2つの矛盾がでるか??

