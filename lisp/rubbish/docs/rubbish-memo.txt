rubbish-memo.txt

[shell]
# ;のない行をwwwに出力する
# kqcからコメントを除去する

grep -v ';' cake005-sim1.kqc |grep -v '^$' >www 


[clause tools]
(pcs {clist})

(p1 cid)

(bcs {clist}) beautiful print

(print-proof0 


[umatrix]
defparameter um1 (make-umatrix *clist*))

全部の述語についてのmgu matriのprintは
(pums um1)

1述語(P)についての3つのmatrixのprint

(pum (assoc 'P um1))


[proof 分析ツール]
(mds)        ; (maxdep (cid ...))
(pph c306)   ; print-hilbert
(oph* (cadr (mds))) 
(ophc fname) ; all contradiction's proof to fname

[log]
(logshow)
  何がNGかわかる


[summary]
#contradictions / #trials を追加したが、意味はあいまい。experimentals
