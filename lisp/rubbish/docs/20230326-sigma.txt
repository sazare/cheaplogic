20230326-sigma

a : P(t)⇒P(t+Δ)

{t+Δ| a}

が、P(t)の真がまわりに滲み出た範囲だから近傍か?

----
t1 > t2　≡ t1 = t2σ

----
σを分解することを考える

σ=∅の場合は、分解できない。
でも

∅ = {(x y) ← (y x)}

なら成り立つ。しかしこれがpar代入なら

{(x y) ← (x x) }

になり、おかしくならない。

xxxx

{x ← f(g(y))}
は
{x ← f(w); w←g(y)}

とできそうだが、par代入だと

(x w)←(f(g(y)) g(y)

と表記するしかない。

だから、σの分解は、par代入の場合、くれぞれの要素のΣiσi=σになり、それとは別の分解はないのか?

合成の順番は問わなければというか、parはそういう作りだから、一意になりそう。

だが、他に同値があるのではないか3/26


