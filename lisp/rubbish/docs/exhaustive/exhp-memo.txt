

10月18日(土)
・canRとcanFにresolve-id, factor-idでFAILしない条件をつけた

・exhp-filter-nofで1つのclause setについてfactor不要かどうか判定する関数
　　noneedfactorをつくり、それを呼ぶようにした

10月16日(木)
パッケージの使い方を考えてみた

(require :exhp)
(in-package :rub)

(rub:readekqc "kqc/exhp/ra004.kqc")
(defvar ss1 *clist*)
(defvar ff1 (nof-subset  ss1))
(defvar rr10 (nof-driver (nth 0 ff1)))
(defvar rr11  (nof-driver (nth 0 ff1)))



(rub:readekqc "kqc/exhp/ra003.kqc")
(defvar ss1 *clist*)
(defvar sss1 (subsetof ss1))
(exhp-filter-nof sss1)


(rub:readekqc "kqc/exhp/ra002.kqc")


10月15日(水)
考えてみたら、部分集合を選んでdriveするのが目的だった。
すべての部分集合でdriveしてもしかたない。
個数についてすこし調べた

(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/ra000.kqc")
 (defparameter ss1 '(c1 c2 c3))
(exhp-filter-nof (subsetof ss1))

(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/ra001.kqc")
 (defparameter ss1 *clist*)
(exhp-filter-nof (subsetof ss1))


;not work in these
(readkqc "kqc/exhp/ra002.kqc")

; test for memory
(readkqc "kqc/exhp/ra020.kqc")
(readkqc "kqc/exhp/ra030.kqc")
(readkqc "kqc/exhp/ra050.kqc")
 (defparameter ss1 *clist*)
(defvar allss (subsetof *clist*))



(nof-driver ss1)
((:SUCCESS (C40)))

(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/ra001.kqc")

(defvar allss (fsubset *clist*))

(nof-driver (nth 10 allss))


10月9日(木)
extract-mguを使ってみる

単純なRのみの fa000.kqc

(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fa000.kqc")

(defparameter ss01 '(c1 c2 c3))
(defparameter pm01 (make-pmap-noF (make-ppnlist ss01)))
(defparameter con01 (proof-driver (car pm01) ss01))





*clist*だとやるたびにかわるからだめ。

結果

RUBBISH(27): con01
(:SUCCESS (C10))

RUBBISH(32): (pcs '(c1 c2 c3 c10))
C1: 1 (X.148 Y.149) ((+ P X.148) (+ Q X.148 Y.149))
C2: 2 NIL ((- P A))
C3: (Z.150 W.151) [((- Q Z.150 W.151))]
C10: RESO = []
NIL

RUBBISH(33): (extract-mgu 'c10 '(c1) '(x.148 y.149))

(:TUPLE A W.177)
RUBBISH(34): (extract-mgu 'c10 '(c3) '(z.150 w.151))

(:TUPLE A W.177)
RUBBISH(35): (print-proof0 'c10)
C10: RESO = []
C10 [] RESOLUTION : <L9-1:L2-1>
  L9-1 (Z.174 W.175).(+ P Z.174)  in
  C9: RESO (Z.174 W.175) ((+ P Z.174))
  C9 (L9-1) RESOLUTION : <L1-2:L3-1>
    L1-2 (X.148 Y.149).(+ Q X.148 Y.149) input1
    C1: 1 (X.148 Y.149) ((+ P X.148) (+ Q X.148 Y.149))
    L3-1 (Z.150 W.151).(- Q Z.150 W.151) input2
    C3: C3 (Z.150 W.151) ((- Q Z.150 W.151))
  L2-1 ().(- P A)  in
  C2: 2 NIL ((- P A))
NIL

あってみたい
Fについてはextract-mguはまだ未対応な気がする


10月8日(水)
make-pmap-noF

;fail
(make-pmap-noF '((Q (L1-2) nil ) (P (L1-1) (L4-1)))
(make-pmap-noF '((Q (nil (L1-2)) (P (L1-1) (L4-1)))
(make-pmap-noF '((Q (L1-2) (L2-3)) (P (nil (L4-1)))
(make-pmap-noF '((Q (L1-2) (L2-3)) (P ((L4-1) nil))


(make-pmap-noF '((Q (L1-2) (L2-2)) (P (L1-1) (L4-1)))
; ((L1-2 L2-2) (L1-1 L4-1))

(make-pmap-noF '((Q (L1-2) (L2-2)) (P (L1-1 L2-1) (L4-1 L3-1)))
; ((L1-2 L2-2) (L1-1 L4-1) (L2-1 L3-1)) 
  ((L1-2 L2-2) (L1-1 L3-1) (L2-1 L4-1)))

(make-pmap-noF '((Q (L3-2 L1-2) (L2-2 L4-1)) (P (L1-1 L2-1) (L4-1 L3-1)))
; ((L3-2 L2-2) (L1-2 L4-1) (L1-1 L4-1) (L2-1 L3-1)) 
((L3-2 L2-2)(L1-2 L4-1) (L1-1 L3-1) (L2-1 L4-1)))
  ((L3-2 L4-1) (L1-2 L4-1)(L1-1 L4-1)(L2-1 L3-1)) 
((L3-2 L4-1) (L1-2 L4-1) (L1-1 L3-1)(L2-1 L4-1)))



10月7日(火)

関数名
  step-forward  =>  step-driver
  exec-prove   => proof-driver

2025/09/28
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fa001.kqc")
(pcs)

(defparameter m1 '((L1-3 'l5-1) (L1-1 L2-1)(L1-2 L3-1))
(defparameter m2 '((L1-3 L5-1) (L1-1 L1-2)(L1-1 L4-1))
 ↑これがだめだ。(L-1が二回)ちいさいほうでいいのか?

((L1-1 L1-2) L4-1)という書き方は??

矛盾なく一貫性のある方法は(どうなっててほしい?)
同じolidを持つlidはどっちでもいいんじゃないか。
だから
(L1-1 L5-1)でも(L1-2 L4-1)でもよいとする。
これをするとRでも複数plid, 複数olidが必要になるので、何か大型のupdateになるのかも
過去に影響がなければ



(make-lidlist *clist*)

*
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fb001.kqc")
(pcs)

(defparameter ss1 '(c1 c2 c3))
(defparameter ss2 '(c1 c2 c4 c5))
(defparameter ss3 '(c1 c2 c4))
(defparameter ss4 '(c1 c2 c5))
(defparameter ss5 '(c1 c2 c6 c7))
(defparameter ss6 '(c1 c2 c7))

*
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fb002.kqc")
(pcs)

(defparameter ss1 '(c1 c2 c9 c10 c11 c3))
(defparameter ss2 '(c1 c2 c9 c10 c11 c4))
(defparameter ss3 '(c1 c2 c9 c10 c11 c5 c6))
(defparameter ss4 '(c1 c2 c9 c10 c11 c7 c9))
(defparameter ss5 '(c1 c2 c9 c10 c11))

*
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fc001.kqc")
(pcs)



2025/09/26
************::10月5日ここ

(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fa003.kqc")

(trace find-lid-in-clist)
(trace resolve-id)
(trace factor-id)
(trace find-lid-in-clist)
(trace find-lid-in-llist)
(trace make-lidlist)

(pcs)

(defparameter ss1  '(c1 c5 c6 c7 c8))
(defparameter pm1 '((l1-3 l5-1)(l1-1 l7-1)(l1-2 l6-1)(l1-4 l8-1)))

(defparameter ss2 '(c1 c2 c8))
(defparameter pm2 '((l1-1 l1-2)(l1-1 l1-3)(l1-1 l2-1)(l1-4 l8-1)))
(defparameter pm3 '((l1-1 l1-3)(l1-1 l1-2)(l1-1 l2-1)(l1-4 l8-1)))
(defparameter pm4 '((l1-2 l1-3)(l1-2 l1-1)(l1-1 l2-1)(l1-4 l8-1)))

(defparameter pm5 '((l1-2 l3-1)(l1-2 l1-1)(l1-1 l2-1)(l1-4 l8-1)))
(defparameter pm6 '((l1-1 l2-1)(l1-3 l1-2)(l1-1 l1-3)(l1-1 l7-1)(l1-2 l6-1)))


(defparameter res nil)
(defparameter mm pm1)
(defparameter cls ss1)

(multiple-value-setq (res mm cls) (step-driver mm cls))

(proof-driver pm1 ss1)
(C32)とか

(defparameter res nil)
(defparameter mm pm5)
(defparameter cls ss2)

(multiple-value-setq (res mm cls) (step-driver mm cls))

(proof-driver pm2 ss2)
(proof-driver pm3 ss2)
(proof-driver pm4 ss2)
;次はfail

(proof-driver pm5 ss2)
(proof-driver pm6 ss2)







(factor-id 'L1-1 'L1-2) 
(factor-id 'L8-1 'L8-2) 
(resolve-id 'l1-1 'l2-1)
(resolve-id 'l8-1 'l3-1)

*
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fa001.kqc")
(pcs)

(defparameter ss1 '(c5 c1 c2))
(defparameter s2 '(c5 c1 c2 c3))
(defparameter s3 '(c5 c1 c2 c4))
(defparameter s3 '(c5 c1 c4))

(defparameter lnp1 (make-lnplist ss1))
(defparameter lnp2 (make-lnplist ss2))
(defparameter lnp3 (make-lnplist ss3))


2025/09/21
*
(require :exhp)
(in-package :rubbish)
(readkqc "kqc/exhp/fa002.kqc")
(pcs)

(defparameter ss1 '(c1 c2 c7))  
(defparameter ss2 '(c1 c3 c4 c7))
(defparameter ss3 '(c1 c5 c6 c7))

(defparameter lnp1 (make-lnplist ss1))
(defparameter lnp2 (make-lnplist ss2))
(defparameter lnp3 (make-lnplist ss3))

(factor-id 'L1-1 'L1-2) 

(resolve-id 'l1-1 'l2-1)
(resolve-id 'l8-1 'l3-1)

(make-lidlist '*clist*)


(loop for x in xx collect (car x))
(sort (loop for x in xx collect (car x)) (lambda (a1 a2) (string< (string a1)(string a2))))
(cons '+Q (loop for pp in (loop for x in xx when (eq '+Q (car x)) collect x)
  collect (cdr pp)))


(make-nplist *clist*)

(defun make-nplist (clist)
  (let (lidlist plist lslidlist mm ltolid)
  (setq lidlist (make-lidlist clist))
  (setq plist   (make-psymlist lidlist))
  (setq lslidlist (loop for lid in lidlist collect (list (lsymof lid) lid)))

  ;through hash table ...
  (setq mm (make-hash-table))
    (loop for pl in xx do 
      (setf (gethash  (car pl) mm) (cons (cdr pl) (gethash (car pl) mm)))
    )
  )
  
   (setq ltolid 
     (loop for p being the hash-keys of mm 
        collect (cons p (gethash p mm))
      )
    )

    (loop for ps in plist collect
      (list ps (assoc (ptolsym '+ psym) itolid)(assoc (ptolsym '- psym) itolid))
    )
)


    (setq lidlist (make-lidlist clist))
    ;; predlist from lidlist
    (setq plist   (make-psymlist lidlist))
    ;; all lidlist to ((lsym lid)...)
    (setq lslidlist (loop for lid in lidlist collect (list (lsymof lid) lid)))

    ;through hash table ..., ((lsym posist neglist)...)
    (setq mm (make-hash-table))
    (loop for pl in xx do
      (setf (gethash  (car pl) mm) (cons (cdr pl) (gethash (car pl) mm)))
    )

    ;; in hash, lsym->lidlist ==> ((lsym poslidlist neglidlist)...
    (setq ltolid
      (loop for p being the hash-keys of mm
        collect (cons p (gethash p mm))
      )
    )
    ; make ((pred poss negs)...)
    (loop for psym in plist collect
      (list psym (cdr (assoc (ptolsym '+ psym) ltolid)) (cdr(assoc (ptolsym '- psym) ltolid)))
    )
  )

(defun getlllist (lsym ids)
  (loop for lid in lids when (eq lsym (lsymof lid)) collect lid)
 )

(defparameter lids (make-lidlist *clist*))
(defparameter pds (make-psymlist lids))

    (loop for psym in plist collect
       (let ((pl  (ptolsym '+ psym) (nl (ptolsym '- psym)))
           
