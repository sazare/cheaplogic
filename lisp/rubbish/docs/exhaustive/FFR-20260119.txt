FFR-20260119


+p,-p,R 
initialでN,M個のとき

述語Pについて

EXHPでは、Rのとき、Fのあと、N=Mでなくてはならない

そのN個にするには、両方の集合でNにできる組み合わせが必要

Fについて
　孤立ぐるーぷ
　　集合要素が、外部と:noのもの
　　　そらの数は、要素間で1個になるなら、

こういう孤立した部分集合は共通要素があるかもしれないので、同値類にはならない

胸痛要素は、どちらかにいれるが、それはやってみないと分からない？


＊Rはリテラルがきえる。Fはリテラルが残る。
＊Rで消えたリテラルのmguはクローズのほかのリテラルに影響する
＊Fは、のこったリテラルにmguの痕跡が残る


Σ全体の静的構造は、この行列の関係になる。


***
ある述語の+と-のリテラル集合をF+とF-とする。
どちらでもいいときはFと書く。

要素数 |F| = N
類に分割したものを(F)と書く。類の数は|(F)|
S1,S2 ∈ (F)について、もしS1∩S2が∅でなければ、その要素はてきとうにS1とS2にふりわけて、同じ要素を含むSiがないようにする。

F+とF-の要素数をN,Mとし N≦Mであるとする。どちらが+/-であるかは問わない。

このN-MがFactoringの必要な数の少ない方の目安になる。

以下は仮説
Factoringは最小回数でよい。
さらにFを繰り返してもinstance化されるだけで、Rには必要がない。
だから、M-N回ですむのならそれでよい。

(F)の中に要素が1つ(Pリテラる)集合があれば、それはFできず、Rによって消滅するしかない。
なのでPはR行列で相手がいなければEXHP失敗になる。
R[P}が複数あれば選択することになる。

(F)の中に複数要素を持つ集合は全部がunifiableで1つになるのであれば、R行列の必要に応じて分割しFactoringすればよい。

★(F)の中の集合たとえば{a,b,c}があって、<a:b>, <a:c>かつb⊥cの場合、{a,b,c}を1つにするFactoringが存在しないので困る

F行列で判定できないか??


**************
F+, F-,Rの関係

exhoではF+とF-の有効な数が同じであること
孤立や、グルーブで他と交わらないものの数が最低の数になる

可能性を取りこぼさない数と実際の数の幅


a,bとa,cでbとcが交わらないとき、仮説のペアはどうなるか

仮説のペアで矛盾しないためには、aはどちらかにxorで

例
a px
b pa
c pb

これは、どちらでも良さそう

ちなみに、グループの作り方で、
bを先にするとa,bとcになる
aを先にするとabcになる

個別なものから始めるのか？

pxy
pab
pcd

は1引数と同じ

pxa
pay
pzz

はどこから始めても一つになる

pxa
pby
pba

もとこから始めても一つ

pxa
pby
pab
はばらばら

pxa
pby
pca
は2つ




